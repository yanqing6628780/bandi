{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch bandi",
            "program": "${workspaceFolder}\\bin/www",
            "outFiles": [
                "${workspaceFolder}/**/*.js"
            ]
        }
    ]
}
